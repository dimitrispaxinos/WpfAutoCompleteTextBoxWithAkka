<Window x:Class="WpfAutoCompleteTextBoxWithAkka.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfAutoCompleteTextBoxWithAkka"
        mc:Ignorable="d"
        DataContext="{StaticResource MainViewModel}"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBox Grid.Row="0" Text="{Binding AutoCompleteTextBoxViewModel.SearchedText, UpdateSourceTrigger=PropertyChanged, Delay=200}"/>
        <ListBox  Grid.Row="1"  ItemsSource="{Binding AutoCompleteTextBoxViewModel.AvailableItems}"/>
    </Grid>
</Window>
